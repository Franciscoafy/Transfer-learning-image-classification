# conda env create --file environment.yml
name: pre_trained_model_dog_and_food
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pandas
  - numpy
  - pip
  - torch
  - torch.nn 
  - pyprojroot
  - python=3.7
  - pip:
    - pyhere
    - datasets 
    - transformers
    - accelerate -U